#ifndef _PARAMETERFILE_H_
#define _PARAMETERFILE_H_
#include <stdlib.h>
#include <stdio.h>
#include <math.h>

void parameterfile(
int ntotal, int nvirt, double *hsml, double *mass, double c,
double B, double g, double alpha, double epsilon, double dt);
#endif
